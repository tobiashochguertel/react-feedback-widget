{
  "name": "react-feedback-widget-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter react-visual-feedback build",
    "dev": "bun run --filter feedback-example dev"
  },
  "devDependencies": {
    "react-visual-feedback": "workspace:*"
  },
  "packageManager": "bun@1.3.5"
}
