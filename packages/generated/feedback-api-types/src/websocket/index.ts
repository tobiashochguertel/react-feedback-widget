/**
 * WebSocket Types - @feedback/api-types
 *
 * Type-safe WebSocket event and command types for the feedback system.
 *
 * DO NOT EDIT THIS FILE DIRECTLY
 * These types are derived from TypeSpec definitions in:
 * - packages/feedback-server-api/typespec/websocket/
 *
 * @module @feedback/api-types/websocket
 *
 * @example
 * ```typescript
 * import {
 *   ServerEvent,
 *   ClientCommand,
 *   isFeedbackCreatedEvent,
 *   createSubscribeCommand,
 * } from '@feedback/api-types/websocket';
 *
 * // Type-safe event handling
 * function handleEvent(event: ServerEvent) {
 *   if (isFeedbackCreatedEvent(event)) {
 *     console.log('New feedback:', event.feedback);
 *   }
 * }
 *
 * // Type-safe command creation
 * const subscribeCmd = createSubscribeCommand('feedback', {
 *   status: ['new', 'in_progress'],
 * });
 * ```
 */

// Re-export all event types
export {
  // Base types
  type BaseServerEvent,
  type Feedback,

  // Event types
  type FeedbackCreatedEvent,
  type FeedbackUpdatedEvent,
  type FeedbackDeletedEvent,
  type FeedbackBulkUpdateEvent,
  type ConnectionAckEvent,
  type SubscriptionConfirmedEvent,
  type ErrorEvent,
  type PongEvent,

  // Union types
  type ServerEvent,
  type ServerEventMap,
  type ServerEventType,

  // Enum types
  type BulkUpdateAction,
  type WebSocketErrorCode,

  // Type guards
  isFeedbackCreatedEvent,
  isFeedbackUpdatedEvent,
  isFeedbackDeletedEvent,
  isFeedbackBulkUpdateEvent,
  isConnectionAckEvent,
  isSubscriptionConfirmedEvent,
  isErrorEvent,
  isPongEvent,
  isFeedbackEvent,
} from './events.js';

// Re-export all command types
export {
  // Base types
  type BaseClientCommand,
  type SubscriptionFilters,

  // Command types
  type SubscribeCommand,
  type UnsubscribeCommand,
  type PingCommand,
  type AuthenticateCommand,

  // Union types
  type ClientCommand,
  type ClientCommandMap,
  type ClientCommandType,

  // Type guards
  isSubscribeCommand,
  isUnsubscribeCommand,
  isPingCommand,
  isAuthenticateCommand,

  // Factory functions
  createSubscribeCommand,
  createUnsubscribeCommand,
  createPingCommand,
  createAuthenticateCommand,
} from './commands.js';
